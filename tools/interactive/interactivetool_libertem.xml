<tool id="interactive_tool_libertem" tool_type="interactive" name="LiberTEM" version="@VERSION@+galaxy0" profile="23.1">
    <icon src="libertem.png" />
    <description>Open Pixelated STEM platform</description>
    <macros>
        <token name="@VERSION@">0.14.2</token>
    </macros>
    <requirements>
        <container type="docker">quay.io/bgruening/libertem:v@VERSION@</container>
    </requirements>
    <entry_points>
        <entry_point name="LiberTEM" requires_domain="True">
            <port>9000</port>
            <url><![CDATA[#action=open&path=/data/]]></url>
        </entry_point>
    </entry_points>
    <command detect_errors="exit_code">
    <![CDATA[
    
        ## $infile is a zip archive and needs to be extracted to /data/ which is in the container
        unzip '$infile' -d /data/ &&
        ls -l /data/ > version.txt &&
    
        /venv/bin/libertem-server --host 0.0.0.0 --insecure --port 9000 &&
        echo "@VERSION@" > version.txt
    ]]>
    </command>
    <inputs>
        <param name="infile" type="data" multiple="true" format="zip" label="Your 4D STEM dataset" />
    </inputs>
    
    <outputs>
        <data name="version" format="txt" label="${tool.name} on ${on_string}: version.txt" from_work_dir="version.txt" />
    </outputs>
    <tests>
    </tests>
    <help><![CDATA[

LiberTEM is an open source platform for high-throughput distributed processing of large-scale binary data sets and live data streams using a modified MapReduce programming model.
The current focus is pixelated scanning transmission electron microscopy (STEM) [MacLaren et al. (2016), Ophus (2019)] and scanning electron beam diffraction data.

]]>
    </help>
    <citations>
        <citation type="doi">10.1017/s1431927619000497</citation>
    </citations>
</tool>
